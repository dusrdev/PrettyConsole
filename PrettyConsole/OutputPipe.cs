namespace PrettyConsole;

/// <summary>
/// An enum representing the output pipe to use
/// </summary>
public enum OutputPipe {
	/// <summary>
	/// Use the standard output pipe
	/// </summary>
	Out,
	/// <summary>
	/// Use the standard error pipe
	/// </summary>
	Error
}